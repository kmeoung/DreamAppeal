<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.truevalue.dreamappeal">

    <!--인터넷 설정-->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 화면 세로 고정 : android:screenOrientation="portrait"-->

        <activity android:name=".activity.ActivityIntro"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ActivityLogin"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ActivityMain"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ActivityDreamTitle"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ActivityDreamDescription"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ActivityMeritAndMotive"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ActivityDreamList"
            android:screenOrientation="portrait"/>

    </application>

</manifest>